# Minimal deps for the deployed dashboard (snapshot mode).
# pyarrow is intentionally omitted — Streamlit ships it as a transitive
# dependency. Declaring it here forces a source build on Python 3.14
# (no cp314 wheel exists for any pyarrow release).

streamlit>=1.39,<2.0
pandas>=2.0,<3.0
numpy>=1.26,<3.0
plotly>=5.20,<7.0

# Optional — only needed for live SPY refresh (snapshot already includes SPY).
alpaca-py>=0.34,<1.0

# Live mode only (DASHBOARD_SOURCE=live). Safe to keep in requirements;
# adds <1MB to the install footprint.
sqlalchemy>=2.0,<3.0
psycopg2-binary>=2.9,<3.0
